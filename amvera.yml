version: null
meta:
  environment: docker
  toolchain:
    name: docker
    version: latest
build:
  dockerfile: Dockerfile
  skip: false
run:
  image: docker
  command: docker-compose up -d
  args: null
  persistenceMount: /data
  containerPort: 443
