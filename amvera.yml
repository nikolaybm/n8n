version: null
meta:
  environment: docker
  toolchain:
    name: docker
    version: latest
build:
  dockerfile: Dockerfile
  skip: false
run:
  image: docker
  command: n8n
  args: null
  persistenceMount: /data
  containerPort: "443"
